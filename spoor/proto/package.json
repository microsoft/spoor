{
  "name": "@microsoft/spoor-proto",
  "version": "0.0.0",
  "description": "Protocol buffers for Spoor traces and function maps.",
  "homepage": "https://github.com/microsoft/spoor/spoor/proto",
  "bugs": {
    "url": "https://github.com/microsoft/spoor/issues"
  },
  "license": "MIT",
  "author": "Microsoft Corporation",
  "repository": {
    "type": "git",
    "url": "https://github.com/microsoft/spoor.git"
  },
  "main": "dist/index",
  "types": "dist/index",
  "files": ["dist"],
  "scripts": {
    "build": "mkdir -p dist && protoc --js_out=import_style=commonjs,binary:dist spoor.proto && tsc",
    "test": "yarn build && jest"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "dependencies": {
    "google-protobuf": "~3.14.0"
  },
  "devDependencies": {
    "@types/jest": "~26.0.19",
    "jest": "~26.6.3",
    "ts-jest": "~26.4.4",
    "typescript": "~4.1.0"
  }
}
